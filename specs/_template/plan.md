# Plan — <feature / version>

> **How** `spec.md` (and `design.md`, if any) get built. Decompose into small tasks, then open
> one GitHub Issue per task. **Status lives in the Issue — do not track it here.** This file is
> a planning snapshot, not a status board.

## Approach / architecture
Key decisions and structure.

## Task breakdown
Each line becomes a GitHub Issue (~0.5–2h, independently shippable):

- <task> — acceptance: …
- <task> — acceptance: …

## Sequencing / dependencies
What must land before what.

## Test strategy
How each task is verified.
