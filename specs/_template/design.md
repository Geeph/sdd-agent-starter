# Design — <feature / version>

> Source of truth for **what the UI must be**. Pairs with `spec.md`.
> Visual source = the prototype app **or** a linked Figma — pick one, don't duplicate.
> Used only on the design track (see AGENTS.md → "Design & prototype track").

## Design goal & principles
What should the experience feel like? Any hard constraints?

## User flows
Step-by-step paths through the product.

## Screens / information architecture
List the screens and how they connect.

## Visual direction
Tone, layout system, key components.

## Design tokens
Colour / spacing / type — link or location (`design/tokens/`).

## Responsive breakpoints
e.g. mobile ≤ 640, tablet, desktop ≥ 1024.

## Accessibility target
e.g. WCAG 2.1 AA.

## Figma (optional)
<link>

## Per-screen acceptance
- [ ] Screen X — …
