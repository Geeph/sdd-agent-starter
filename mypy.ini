[mypy]
# CI runs `mypy .` whenever a Python project is present. Ignore missing third-party stubs so
# the gate fails on real type errors, not on un-stubbed imports. Tighten per-project as needed.
ignore_missing_imports = True
